# Catalog data

Each resource is one YAML file to keep reviews focused and Git history interpretable. Records distinguish provider ownership, access procedure, commercial-use status, redistribution status, source relationship, citation, and verification state.

`verified` means the listed core facts were checked against an authoritative source on the stated date. `partial` means one or more legal/access/implementation details require re-verification. It is not a quality judgment about the resource.
