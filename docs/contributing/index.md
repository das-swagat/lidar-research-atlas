# Contributing

Use one focused pull request per resource or tightly related correction. Primary sources and conservative wording are required. Promotional submissions, scraped lists, copied abstracts, and unverified “official” claims are rejected.

Run:

```bash
python scripts/validate_catalog.py
python scripts/build_catalog.py
python scripts/check_no_restricted_files.py
pytest
```
