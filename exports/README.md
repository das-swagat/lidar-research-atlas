# Frozen catalog exports

These files contain **atlas-created metadata only**. They include no LiDAR scans, labels, images, model weights, credentials, or restricted provider files.

For scholarly deposit, archive the complete tagged release and describe the exports as curated metadata. Cite original datasets and methods independently. The provider terms linked in each record control use of the underlying third-party resource.
